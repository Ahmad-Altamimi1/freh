/**
 * The single theme this app ships with.
 *
 * The value must match the `[data-theme='…']` selector in the matching file
 * under `src/styles/themes/`, which is where the OKLCH tokens live. It is set
 * statically on `<html>` in `src/app/layout.tsx`.
 *
 * To swap themes, add the CSS file, import it in `src/styles/theme.css`, and
 * change this value. See docs/themes.md.
 */
export const DEFAULT_THEME = 'thaqafa';
